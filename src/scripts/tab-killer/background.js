import JSXComponent from 'metal-jsx';

class Background extends JSXComponent {
  attached() {
    this.init();
  }

  updateBadgeText() {
  }

  displayResults(window_list) {
  }

  updateBadgeTitle(count) {
  }

  getAllStats(callback) {
  }

  init() {
  }

  render() {
    return
  }
}

Background.STATE = {
  windowList: {},
  allWindowsTabCount: 0,
};

export default Background;
